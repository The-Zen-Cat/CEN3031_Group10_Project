import React from 'react';

//This is a placeholder page for testing purposes which should load the page when a user
//attempts to access the User Account Page.
//This is a future roadmap feature.
const Account = () => {
  return (
    <div>
      <h1>
        This is the User Account page. It will show the users personal information and allow them to
        update it.
      </h1>
    </div>
  );
};

export default Account;
