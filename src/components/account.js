import React from 'react';

const Account = () => {
    return (
        
        <div>
            <h1>This is the User Account page. It will show the users personal information and allow them to update it.</h1>
        </div>
        
    );
}

export default Account;